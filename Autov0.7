--// TRZ HUB UI FRAMEWORK

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")

local Player = Players.LocalPlayer

if CoreGui:FindFirstChild("TRZHUB") then
    CoreGui.TRZHUB:Destroy()
end

-- GUI
local Gui = Instance.new("ScreenGui")
Gui.Name = "TRZHUB"
Gui.Parent = CoreGui

-- OPEN BUTTON
local Open = Instance.new("TextButton")
Open.Parent = Gui
Open.Size = UDim2.new(0,60,0,60)
Open.Position = UDim2.new(0,20,0.5,-30)

Open.Text = "TRZ"
Open.Font = Enum.Font.GothamBold
Open.TextSize = 18

Open.BackgroundColor3 = Color3.fromRGB(20,20,20)
Open.TextColor3 = Color3.new(1,1,1)

Instance.new("UICorner",Open).CornerRadius = UDim.new(1,0)

-- MAIN
local Main = Instance.new("Frame")
Main.Parent = Gui

Main.Size = UDim2.new(1,0,1,0)
Main.BackgroundColor3 = Color3.fromRGB(15,15,25)

-- GRADIENT
local Gradient = Instance.new("UIGradient")
Gradient.Parent = Main

Gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(
        0,
        Color3.fromRGB(0,170,255)
    ),

    ColorSequenceKeypoint.new(
        1,
        Color3.fromRGB(255,60,60)
    )
}

Gradient.Rotation = 45

-- TRANSPARENCY
Main.BackgroundTransparency = 0.25

-- TITLE
local Title = Instance.new("TextLabel")
Title.Parent = Main

Title.Size = UDim2.new(1,0,0,80)
Title.Position = UDim2.new(0,0,0,50)

Title.BackgroundTransparency = 1

Title.Text = "TRZ AUTO FARM CHESTS"

Title.TextColor3 = Color3.new(1,1,1)

Title.Font = Enum.Font.GothamBlack
Title.TextSize = 38

-- STATUS
local Status = Instance.new("TextLabel")
Status.Parent = Main

Status.Size = UDim2.new(1,0,0,40)
Status.Position = UDim2.new(0,0,0,120)

Status.BackgroundTransparency = 1

Status.Text = "STATUS : IDLE"

Status.TextColor3 = Color3.fromRGB(220,220,220)

Status.Font = Enum.Font.Gotham
Status.TextSize = 20

-- LOG FRAME
local Logs = Instance.new("Frame")
Logs.Parent = Main

Logs.Size = UDim2.new(0,500,0,250)
Logs.Position = UDim2.new(0.5,-250,0.5,-125)

Logs.BackgroundColor3 = Color3.fromRGB(10,10,15)
Logs.BackgroundTransparency = 0.2

Instance.new("UICorner",Logs).CornerRadius = UDim.new(0,15)

local Stroke = Instance.new("UIStroke")
Stroke.Parent = Logs

Stroke.Color = Color3.fromRGB(255,220,120)
Stroke.Thickness = 2

-- INFO
local Info = Instance.new("TextLabel")
Info.Parent = Logs

Info.Size = UDim2.new(1,-20,1,-20)
Info.Position = UDim2.new(0,10,0,10)

Info.BackgroundTransparency = 1

Info.TextXAlignment = Enum.TextXAlignment.Left
Info.TextYAlignment = Enum.TextYAlignment.Top

Info.Text =
[[

TRZ HUB LOADED

> AUTO CHEST
> FPS BOOST
> ANTI AFK
> AUTO REJOIN

STYLE :
BANANA KAITUN INSPIRED

]]

Info.TextColor3 = Color3.new(1,1,1)

Info.Font = Enum.Font.Code
Info.TextSize = 18

-- OPEN/CLOSE
local Visible = true

Open.MouseButton1Click:Connect(function()

    Visible = not Visible

    Main.Visible = Visible

end)
